int recursiva();

int main()
{
    printf("Resultado = %d", recursiva(4));
    return 0;
}

int recursiva(int x){
    if(x==1){
        return -x;
    }else{
        return -5*recursiva(x-1)+x; //125+40-1
    }
}
